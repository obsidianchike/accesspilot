'use client'

// ─────────────────────────────────────────────
// CoachSummary — Coach Agent output panel
//
// Shows:
//   • Plain-English overview of the scan
//   • Prioritised action list
//   • Learning resources / tips
//
// Props:
//   coachSummary — { overview, priorityList[], learningTips[] }
// ─────────────────────────────────────────────

export default function CoachSummary({ coachSummary }) {
  if (!coachSummary) return null

  const { overview, priorityList = [], learningTips = [] } = coachSummary

  return (
    <div className="bg-gradient-to-br from-pilot-50 to-white border border-pilot-200 rounded-3xl p-6 sm:p-8 shadow-sm">

      {/* Header */}
      <div className="flex items-center gap-3 mb-5">
        <div className="w-11 h-11 rounded-xl bg-pilot-500 flex items-center justify-center
                        text-white text-xl flex-shrink-0" aria-hidden="true">
          🎯
        </div>
        <div>
          <h2 className="font-display text-lg font-bold text-slate-900">
            AI Coach Summary
          </h2>
          <p className="text-sm text-pilot-600">
            Generated by AccessPilot Coach Agent · Powered by Azure OpenAI GPT-4o
          </p>
        </div>
      </div>

      {/* Overview */}
      <div className="mb-6">
        <p className="text-slate-700 leading-relaxed text-sm">
          {overview}
        </p>
      </div>

      {/* Priority actions + Learning tips side by side on desktop */}
      <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">

        {/* Priority actions */}
        {priorityList.length > 0 && (
          <div>
            <h3 className="font-semibold text-slate-900 text-sm mb-3 flex items-center gap-2">
              <span className="w-5 h-5 rounded-md bg-serious-500 flex items-center justify-center
                               text-white text-xs font-bold flex-shrink-0" aria-hidden="true">
                !
              </span>
              Priority Actions
            </h3>
            <ol className="space-y-2" aria-label="Priority accessibility actions">
              {priorityList.map((action, i) => (
                <li key={i} className="flex items-start gap-3 text-sm">
                  <span
                    className="flex-shrink-0 w-5 h-5 rounded-full bg-pilot-100 text-pilot-700
                               flex items-center justify-center text-xs font-bold mt-0.5"
                    aria-hidden="true"
                  >
                    {i + 1}
                  </span>
                  <span className="text-slate-700 leading-relaxed">{action}</span>
                </li>
              ))}
            </ol>
          </div>
        )}

        {/* Learning tips */}
        {learningTips.length > 0 && (
          <div>
            <h3 className="font-semibold text-slate-900 text-sm mb-3 flex items-center gap-2">
              <span className="w-5 h-5 rounded-md bg-minor-500 flex items-center justify-center
                               text-white text-xs flex-shrink-0" aria-hidden="true">
                📚
              </span>
              Learning Resources
            </h3>
            <ul className="space-y-2" aria-label="Learning tips and resources">
              {learningTips.map((tip, i) => (
                <li key={i} className="flex items-start gap-3 text-sm">
                  <svg
                    className="flex-shrink-0 w-4 h-4 text-minor-500 mt-0.5"
                    fill="none" stroke="currentColor" viewBox="0 0 24 24"
                    strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true"
                  >
                    <path d="M20 6L9 17l-5-5"/>
                  </svg>
                  <span className="text-slate-700 leading-relaxed">{tip}</span>
                </li>
              ))}
            </ul>
          </div>
        )}
      </div>

      {/* Disclaimer */}
      <div className="mt-6 pt-4 border-t border-pilot-200 text-xs text-pilot-500 flex items-start gap-2">
        <svg className="w-3.5 h-3.5 shrink-0 mt-0.5" fill="none" stroke="currentColor"
          viewBox="0 0 24 24" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">
          <circle cx="12" cy="12" r="10"/>
          <path d="M12 16v-4M12 8h.01"/>
        </svg>
        This summary was generated by Azure OpenAI GPT-4o and should be reviewed alongside
        manual accessibility testing. Automated tools detect ~30–40% of WCAG issues.
      </div>
    </div>
  )
}
